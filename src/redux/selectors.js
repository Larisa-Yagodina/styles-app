export const getIsOpenModal = (state) => state.modalReducer.isOpenModal;
export const getIsAuth = (state) => state.modalReducer.isAuth;
export const getModalType = (state) => state.modalReducer.type;

export const getIndex = (state) => state.modalReducer.imageIndex;

export const getImages = (state) => state.modalReducer.images;

